"""SensorLab's models.

Empty in SL-A1 on purpose: this sprint builds the walls, not the contents.
`SensorLabProfile` arrives in SL-A2 (it is what the language switch persists
into), and the curriculum models in SL-B1. See docs/sensorlab/data_model.md.
"""
