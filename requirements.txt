Django==5.2.15
djangorestframework>=3.15,<3.16
gunicorn>=22,<24
whitenoise>=6.7,<7
django-allauth[socialaccount]>=65.0,<66
requests>=2.31,<3
Pillow>=10.0,<12
# memz's own dependency (ACT-Z.12, 2026-09-16): reads a font's cmap table
# to know exactly which characters it can draw, so a caption's emoji get
# dropped before rendering instead of baking in a broken-glyph box.
fonttools>=4.50,<5
qrcode>=7,<9
# Pinned exactly (2026-09-16), not a range: a loose ">=0.6,<1" let dev and
# production resolve different builds, which rendered the same Hebrew
# caption differently (a real right-to-left reversal on production, not
# reproducible against any version installed locally) -- see memz's own
# spec.md §8.1 for the fuller story.
python-bidi==0.6.10
python-dotenv>=1.0,<2
openai>=1.30,<2
django-anymail[resend]>=12.0,<13
markdown>=3.6,<4
yt-dlp>=2026.6.9
imageio-ffmpeg>=0.5,<1
pytest-django>=4.9,<5
google-auth>=2.20,<3
black>=24.0,<26
ruff>=0.4,<1
pre-commit>=3.7,<5
# Web Push (REQ-11.6.9): browser notifications that arrive with the browser
# closed. VAPID keys live in env vars, never in the repo.
pywebpush>=2.0
